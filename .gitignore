# Rendered artifacts
output.png
output.html
out-*.png
out/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
dist/
build/
*.egg-info/

# Local config / secrets (the deploy repo owns these).
# The .example.yml stays in git; the live docker-compose.yml does not.
# secrets/ holds OAuth credentials + refresh tokens.
.env
config.local.yaml
config.og.yaml
docker-compose.yml
secrets/

# Tooling
.DS_Store
.idea/
.vscode/
